//! Library surface of the glpv CLI crate: the fixture-repo builder, shared by
//! the integration tests and the `build_fixtures` example (which materialises
//! the demo projects).

pub mod fixtures;
