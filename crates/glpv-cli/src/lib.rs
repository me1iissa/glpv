//! Library surface of the glpv CLI crate: the fixture-repo builder (shared by
//! the integration tests and the `build_fixtures` example) and the oracle
//! comparison behind `glpv check`.

pub mod check;
pub mod fixtures;
