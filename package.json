{
  "name": "glpv-ui-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Headless tests for the glpv viewer: JS/wasm evaluator parity and a jsdom smoke harness. Not published.",
  "scripts": {
    "prepare-samples": "node ui/test/prepare.mjs",
    "pretest": "node ui/test/prepare.mjs",
    "test": "node --test \"ui/test/*.test.mjs\""
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  },
  "optionalDependencies": {
    "canvas": "^3.2.3"
  }
}
